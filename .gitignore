# senhas de ramal em texto plano - não versionar
provisioning/files/*.cfg
asterisk/certs/*.key

# lixo de execução dos testes
__pycache__/
*.pyc
.pytest_cache/

# gravações de chamadas reais - nunca versionar
recordings/*
!recordings/.gitkeep

# painel de administracao - dados gerados contêm senhas SIP em texto
# plano; os arquivos versionados são só os placeholders vazios
admin-api/data/
